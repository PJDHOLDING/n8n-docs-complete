name: Sync upstream (mirror)
on:
  schedule:
    - cron: "0 */6 * * *"   # toutes les 6h
  workflow_dispatch:        # lancement manuel

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (fork)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git (bot)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Add upstream & fetch
        run: |
          git remote | grep -q upstream || git remote add upstream https://github.com/digibooms25/n8n-docs-complete.git
          git fetch upstream main

      - name: Mirror upstream/main -> origin/main
        run: |
          git checkout -B main
          git reset --hard upstream/main
          git push origin main --force


Add workflow to sync fork with upstream
